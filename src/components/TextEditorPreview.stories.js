import { html } from 'lit';
import './TextEditor.js'; // or adjust path if it's in another folder

export default {
  title: 'Components/TextEditor',
  component: 'text-editor',
};

export const Default = () => html`<text-editor></text-editor>`;
