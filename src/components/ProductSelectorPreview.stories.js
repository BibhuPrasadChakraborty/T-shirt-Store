import { html } from 'lit';
import './ProductSelector.js'; // or adjust path if it's in another folder

export default {
  title: 'Components/ProductSelector',
  component: 'product-selector',
};

export const Default = () => html`<product-selector></product-selector>`;
