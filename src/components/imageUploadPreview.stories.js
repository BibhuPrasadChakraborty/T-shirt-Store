import { html } from 'lit';
import './imageUpload.js'; // or adjust path if it's in another folder

export default {
  title: 'Components/imageUpload',
  component: 'image-upload',
};

export const Default = () => html`<image-upload></image-upload>`;
